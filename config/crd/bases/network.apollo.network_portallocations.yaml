---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: portallocations.network.apollo.network
spec:
  group: network.apollo.network
  names:
    kind: PortAllocation
    listKind: PortAllocationList
    plural: portallocations
    shortNames:
    - pa
    singular: portallocation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.networkPoolRef.name
      name: Pool
      type: string
    - jsonPath: .spec.podInfo.name
      name: Pod
      type: string
    - jsonPath: .spec.podInfo.namespace
      name: Namespace
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.bindingStatuses[*].phase
      name: Bindings
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: PortAllocation is the Schema for tracking individual port allocations
          and their binding status
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PortAllocationSpec defines the desired state of PortAllocation
            properties:
              podInfo:
                description: PodInfo contains the basic information of the pod
                properties:
                  name:
                    description: Name is the name of the pod
                    type: string
                  namespace:
                    description: Namespace is the namespace of the pod
                    type: string
                  podIP:
                    description: PodIP is the IP address of the pod
                    type: string
                  uid:
                    description: UID is the UID of the pod for strong reference
                    type: string
                required:
                - name
                - namespace
                type: object
              portBindings:
                description: PortBindings contains the detailed port binding information
                items:
                  description: PortBinding defines the binding information between
                    pod port and load balancer port
                  properties:
                    bindingConfig:
                      description: BindingConfig contains additional binding configuration
                      properties:
                        sessionAffinity:
                          description: SessionAffinity defines session affinity configuration
                          type: string
                        targetWeight:
                          description: TargetWeight is the weight for load balancing
                          format: int32
                          type: integer
                      type: object
                    bindingType:
                      description: BindingType defines the type of load balancer binding
                      type: string
                    lbPort:
                      description: LBPort is the allocated load balancer port that
                        maps to the pod port
                      format: int32
                      type: integer
                    loadBalancerRef:
                      description: LoadBalancerRef references the specific load balancer
                      properties:
                        id:
                          description: ID is the unique identifier of the load balancer
                          type: string
                        provider:
                          description: Provider is the cloud provider (e.g., "volcengine")
                          type: string
                        region:
                          description: Region is the region where the load balancer
                            is located
                          type: string
                        resourceGroup:
                          description: ResourceGroup is the resource group name (if
                            applicable)
                          type: string
                      required:
                      - id
                      - region
                      type: object
                    podPort:
                      description: PodPort is the port number exposed by the pod/container
                      format: int32
                      type: integer
                    portName:
                      description: PortName is the name of the port (from container
                        port definition)
                      type: string
                    protocol:
                      description: Protocol specifies the network protocol for this
                        port mapping
                      type: string
                  required:
                  - bindingType
                  - lbPort
                  - loadBalancerRef
                  - podPort
                  - protocol
                  type: object
                type: array
            required:
            - podInfo
            - portBindings
            type: object
          status:
            description: PortAllocationStatus defines the observed state of PortAllocation
            properties:
              allocatedAt:
                description: AllocatedAt is the timestamp when the ports were allocated
                format: date-time
                type: string
              bindingStatuses:
                description: BindingStatuses contains the binding status for each
                  port
                items:
                  description: BindingStatus represents the binding status for a specific
                    port
                  properties:
                    boundAt:
                      description: BoundAt is the timestamp when the port was successfully
                        bound
                      format: date-time
                      type: string
                    lastProbeTime:
                      description: LastProbeTime is the last time the binding was
                        checked
                      format: date-time
                      type: string
                    lbPort:
                      description: LBPort is the load balancer port number
                      format: int32
                      type: integer
                    loadBalancerConfig:
                      additionalProperties:
                        type: string
                      description: LoadBalancerConfig contains the actual configuration
                        applied to load balancer
                      type: object
                    message:
                      description: Message provides human-readable details about the
                        binding status
                      type: string
                    phase:
                      description: Phase represents the current binding phase
                      type: string
                    podPort:
                      description: PodPort is the pod port number
                      format: int32
                      type: integer
                  required:
                  - lbPort
                  - phase
                  - podPort
                  type: object
                type: array
              boundAt:
                description: BoundAt is the timestamp when all ports were successfully
                  bound
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the allocation's current state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastUpdated:
                description: LastUpdated is the timestamp of the last status update
                format: date-time
                type: string
              message:
                description: Message provides human-readable details about the overall
                  allocation status
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed spec
                format: int64
                type: integer
              phase:
                description: Phase represents the current overall phase of the port
                  allocation
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
